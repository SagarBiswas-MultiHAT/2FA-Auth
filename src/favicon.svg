<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" viewBox="0 0 64 64">
  <defs>
    <radialGradient id="g" cx="30%" cy="30%" r="80%">
      <stop offset="0%" stop-color="#4884ff" stop-opacity="0.95"/>
      <stop offset="100%" stop-color="#2f66ff" stop-opacity="0.95"/>
    </radialGradient>
  </defs>

  <!-- Background disc (matches .brand gradient) -->
  <circle cx="32" cy="32" r="30" fill="url(#g)"/>

  <!-- Inner ring highlight -->
  <circle cx="32" cy="32" r="20" fill="#ffffff" opacity="0.22"/>

  <!-- Brand mark (matches .brand__mark silhouette) -->
  <g transform="rotate(18 32 32)">
    <path
      d="M26 26 L38 26 L35 32 L38 38 L26 38 L29 32 Z"
      fill="#ffffff"
      opacity="0.92"
      stroke="none"
    />
  </g>
</svg>
